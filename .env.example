# 인터넷에 올릴 때만 쓰는 값. 교실 LAN 에서는 아무것도 필요 없다.

# 학생에게 알려 줄 주소. QR 도 이 주소를 가리킨다.
# 비워 두면 서버가 로컬 IP 를 자동으로 찾는다 (교실 LAN·핫스팟 모드).
PUBLIC_URL=https://trust-village.onrender.com

# 호스팅 업체가 알아서 넣어 주는 값. 직접 건드릴 일은 없다.
# PORT=3000
