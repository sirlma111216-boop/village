# 신뢰마을 (Trust Village)

중학교 한 학급(20~34명)이 60분 수업 동안 함께하는 **실시간 게임 웹앱**.
주소 하나로 접속하는 평범한 웹앱이고, 학생은 폰 브라우저로 들어온다.
"들키지 않으면 괜찮을까?"라는 질문을 몸으로 겪게 하는 도덕/사회 수업 도구.

---

## 0. 절대 원칙 (Non-negotiable)

| # | 원칙 |
|---|---|
| 1 | **개인정보 0 수집.** 이름·학번·전화번호·이메일·로그인 없음. 학생 식별은 서버가 자동 배정하는 익명 닉네임(형용사+동물)뿐. |
| 2 | **개인 선택 비공개.** 누가 무엇을 골랐는지는 어떤 화면에도, 진행자에게도, 로그/스냅샷/CSV에도 남기지 않는다. 서버 메모리에만 두고 집계 후 카운트만 보존. |
| 3 | **평범한 웹앱.** 배포한 주소로 누구나 접속한다. 진행자가 보고 있는 주소를 서버가 그대로 읽어 QR 을 만들므로 설정할 환경변수가 없다. 외부 API·생성형 AI 호출은 쓰지 않는다(폰트도 로컬 번들). |
| 4 | **외부 DB 금지.** 수업 하나 = Durable Object 하나. 상태는 그 안의 메모리 + DO 저장소. 재시작·하이버네이션 뒤에도 복구. |
| 5 | **서버가 단일 진실 소스.** 진행자/학생 화면 새로고침에도 상태 유지. |
| 6 | **특정 브랜드명 금지.** 시나리오에 실제 메신저/상점/앱 이름을 쓰지 않는다. |

---

## 1. 기술 스택 (고정)

- **런타임**: Cloudflare Workers + Durable Objects. 수업 하나가 DO 하나이고, 그 안에 상태와 WebSocket 이 함께 있다.
- **프런트**: 빌드 도구 없는 순수 HTML/CSS/JS (ES modules). 같은 서버에서 정적 서빙.
- **실시간**: 표준 WebSocket + 작은 JSON 규약(worker/protocol.js). 라이브러리 없음.
- **의존성**: qrcode(순수 JS 경로로 SVG 생성)와 폰트 두 개뿐.
- **폰트/아이콘**: 로컬 번들 또는 시스템 폰트 + 유니코드 이모지만 사용 (무료 라이선스).

---

## 2. 게임 규칙

### 2.1 세션 · 마을
- 진행자가 세션 생성 → **6자리 코드** 발급 (혼동 문자 0/O/1/I/L 제외). 코드가 곧 Durable Object 이름이다.
- 학생은 QR 또는 코드로 입장. **입장 순서대로 마을에 라운드로빈 배정**(마을 수 2~6, 기본 4).
- 학생 개인 자산 = **코인**, 마을 자산 = **신뢰지수**(0~100, 시작 60).

### 2.2 라운드 선택지
각 라운드에서 학생은 3개 중 **하나를 비밀리에** 고른다.

| 선택 | 이름 | 개인 코인 | 마을 신뢰 | 학생 화면 표기 |
|------|------|-----------|-----------|----------------|
| a | 몰래 이득 | **+3** | **-2** | 마을 영향 `???` |
| b | 규칙대로 | **+1** | **+1** | 그대로 표시 |
| c | 용기 내어 알리기 | **0** | **+3** | 그대로 표시 |

### 2.3 신뢰지수 계산 (마을 크기 공정성)
```
raw   = Σ(선택별 신뢰 델타)          // a:-2, b:+1, c:+3
delta = round( raw / 마을인원 * NORM )  // NORM = 4 (기준 마을 크기)
신뢰지수 = clamp(신뢰지수 + delta, 0, 100)
```
미제출자는 분모에서 제외하지 않되 델타 0으로 취급(= 무행동). 인원 0이면 delta 0.

### 2.4 마을회의 (2라운드 후)
각 마을이 제도 카드 3개 중 하나를 토의 후 **다수결 채택**. 동점이면 카드 순서상 앞선 것. 채택 제도는 **3·4라운드에만** 적용.

| 제도 | 효과 |
|------|------|
| 📖 투명 장부제 | 라운드 진행 중 **우리 마을**의 선택 비율(a/b/c %)이 실시간 공개(익명 집계만). |
| 🔍 청렴 감사제 | `a` 선택은 **40% 확률로 적발** → 개인 코인 **-4**. 적발 사실은 **그 학생 화면에만** 표시. |
| ✋ 청렴 서약제 | 라운드 시작 전 서약 버튼을 누른 학생이 `b`/`c`를 고르면 개인 코인 **+1** 보너스. |

### 2.5 순위
- **최종 순위 = 마을 신뢰지수만으로** 매긴다.
- 개인 코인 랭킹은 **익명 닉네임**으로 참고용만 표시(반전 연출용: 코인 1등이 신뢰 꼴찌 마을일 수 있음).

---

## 3. 단계 상태 머신 (진행자 "다음" 버튼으로만 전진)

```
lobby → warmup → story → rules → practice
      → round1 → round2 → interim → council
      → round3 → round4 → reveal → reflect → pledge → end
```

| 단계 | 내용 |
|------|------|
| `lobby` | QR·코드·실시간 입장 인원 |
| `warmup` | 워밍업 투표 2문항 |
| `story` | 기게스의 반지 웹툰 3컷 |
| `rules` | 규칙 안내 |
| `practice` | 연습 라운드(떡볶이 거스름돈) — 점수 미반영 |
| `round1~4` | 시나리오 라운드 (기본 60초, 진행자 연장/스킵) |
| `interim` | 중간 집계 |
| `council` | 마을회의(제도 투표) |
| `reveal` | 최종 발표 — 개인 코인 → 마을 코인 → 신뢰지수 순위, 버튼마다 한 단계씩 공개 |
| `reflect` | 익명 소감 벽 + 하트 |
| `pledge` | 서약 |
| `end` | 종료 · CSV 다운로드 |

---

## 4. 화면

### 4.1 진행자 (노트북/프로젝터 — 큰 글씨·고대비)
- 세션 생성 → 접속 QR(**지금 접속한 주소 자동 반영**) + 6자리 코드 + 실시간 입장 인원
- 라운드 화면: 시나리오 웹툰 + 지문 + 남은 시간 + **제출 인원 실시간 카운트**(누가 냈는지는 표시하지 않음)
- 집계 화면: 마을별 신뢰지수 **가로 막대(애니메이션)**, 개인 코인 TOP3(익명)
- 최종 발표: 라운드별 **정직 선택률(%)** 막대 그래프 + **마을회의 시점 표시선**
- 소감 벽: 익명 소감 카드가 실시간 추가, 하트 수 표시
- 서약: 서약 문구 + 인원 카운터 차오르는 연출
- 설정: 마을 수 / 라운드 시간 / 시나리오 세트 / **데모 봇 모드**
- 종료 후 **CSV 다운로드**(개인 식별 없는 집계만)

### 4.2 학생 (스마트폰 세로 — 큰 터치 영역)
- 코드 입장 → 닉네임·마을 확인 → 대기
- 라운드: 시나리오 카드 + 선택 버튼 3개 + 남은 시간 → 제출 시
  **"제출 완료 — 네 선택은 아무도 볼 수 없어"** 잠금 화면
- 라운드 결과: 내 코인 변화, 우리 마을 신뢰지수 변화, **내 선택은 나에게만** 표시
- 마을회의: 제도 카드 3개 + 득표수 + 투표 버튼
- 소감 입력(익명·100자), 하트 누르기, 서약 버튼

### 4.3 공통 디자인
한국어 UI · 흑백 편집 지면 + 커다란 파스텔 색 블록 · 알약 버튼 · 큰 터치 영역 · 가벼운 애니메이션.
토큰은 `public/css/base.css` 한 곳. (디자인 근거: Figma 마케팅 사이트 분석 문서)

---

## 5. 콘텐츠 (교체 가능하도록 `content/` 아래 JSON 분리)

- `content/scenarios.json` — 시나리오 세트. 각 항목: `id`, `title`, `story`(2~3문장, 중학생 말투), `image`(`public/illustrations/` 안의 파일 이름, 확장자 없이), `emoji`·`tone`(파일이 없을 때 쓰는 폴백), `choices[3]`(a/b/c 문구)
  - 연습: **떡볶이 거스름돈**
  - R1 **급식 새치기 찬스** / R2 **심판의 유혹** / R3 **학급비의 비밀** / R4 **시험지 쪽지**
- `content/warmup.json` — 워밍업 2문항
- `content/story.json` — 기게스의 반지 3컷 텍스트
- `content/institutions.json` — 제도 카드 3종

---

## 6. 안정성 요구사항

- **학생 재접속**: `localStorage`에 세션 토큰 저장 → 절전/새로고침 시 같은 닉네임·마을·진행 상태로 자동 복구.
- **늦은 입장**: 진행 중인 단계에 즉시 합류.
- **진행자 새로고침**: 서버 상태로 완전 복원 (localStorage 의 코드·열쇠로 자동 재접속).
- **34명 동시 제출**: 이벤트는 단순하게, **브로드캐스트는 집계값만**(개인 이벤트는 해당 소켓에만).
- **하이버네이션 대비**: Durable Object 가 잠들었다 깨어나도 소켓 역할과 진행 중인 라운드가 이어진다.

---

## 7. 디렉터리 구조

```
village/
├── CLAUDE.md  README.md  wrangler.jsonc
├── worker/               # Cloudflare 진입점
│   ├── index.js          #   정적 파일 · /api/* · /ws → Durable Object
│   ├── session-do.js     #   수업 하나 = DO 하나 (상태·소켓·저장·알람)
│   ├── protocol.js       #   WebSocket 메시지 규약
│   └── qr.js             #   QR (SVG, 순수 JS)
├── shared/               # 런타임과 무관한 게임 두뇌 (Node·Workers 공용)
│   ├── config.js  content.js
│   ├── game/             #   session · stages · engine · demo · villages
│   └── lib/              #   nickname · code · clean
├── public/               # 빌드 없는 프런트 (Workers Assets 가 서빙)
│   ├── index.html  host.html
│   └── css/  js/  fonts/  illustrations/
├── content/              # 교체 가능한 JSON 콘텐츠
└── tools/                # 점검 스크립트
```

---

## 8. 개발 단계

1. **뼈대 + 세션 생성/입장/마을 배정 + QR**
2. 라운드 엔진(타이머·비밀 선택·집계·점수) + 상태 머신
3. 진행자 화면 전체 플로우
4. 학생 화면 전체 플로우 + 재접속 복구
5. 마을회의·제도 효과 + 최종 발표 연출 + 소감/서약
6. 콘텐츠 주입, 데모 봇 모드, CSV 내보내기, 마무리

---

## 8.5 데모 봇 모드 (리허설 · 시연)

진행자 설정 또는 진행 중 `데모 봇` 버튼으로 켠다. 가상 학생 28명이 **서버 안에서** 돌며
단계마다 알아서 움직인다 (워밍업 · 라운드 · 마을회의 · 소감 · 하트 · 서약).

분포는 이 수업이 보여 주려는 곡선을 재현한다.

| 라운드 | 몰래 이득 | 정직 선택률 |
|---|---|---|
| 연습 · R1 · R2 | 다수 (52~70%) | 30~40% |
| R3 · R4 | 소수 (30~38%) | 60~85% |

제도가 붙으면 "몰래 이득"에서 정직 쪽으로 옮겨 간다 — 감사제 0.22 · 장부제 0.16 · 서약제 0.13.
몇 명(7%)은 일부러 제출하지 않아 미제출자 처리 경로까지 시연에 드러난다.
구현은 `shared/game/demo.js`.

---

## 9. 실행

```bash
npm install
npm run dev        # http://localhost:8787
npm run deploy     # Cloudflare 에 올린다
```
진행자: `/host` · 학생: `/` (QR 이 지금 접속한 주소를 그대로 가리킴)
